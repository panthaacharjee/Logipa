import React from "react";
import classes from "../styles/footer.module.css";

const Footer = () => {
  return (
    <footer className={classes.container}>
      <h1>Footer same as landing page</h1>
    </footer>
  );
};

export default Footer;
